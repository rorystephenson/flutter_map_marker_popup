name: flutter_map_marker_popup_example
description: Example for Flutter Map Marker Popups.
publish_to: none

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2

  latlong: ">=0.6.0"
  flutter_map: ">=0.12.0 <1.0.0"
  flutter_map_marker_popup:
    path: ../

flutter:
  uses-material-design: true

dev_dependencies:
  pedantic: ^1.9.0
  flutter_test:
    sdk: flutter